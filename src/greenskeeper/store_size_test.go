package greenskeeper_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("StoreSize", func() {
	It("returns the size of the volume containing the store path minus 15GB", func() {

	})

	PContext("when the disk containing the store path is smaller than 15GB", func() {})
})
